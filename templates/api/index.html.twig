{% extends 'base.html.twig' %}

{% block title %}Controller test Api{% endblock %}

{% block body %}

<div class="row">

    <div class="col-6">
        <form method="POST">
                <div class="form-group">
                    <label for="input">Choix du dé :</label>
                    <input type="text" name="input" class="form-control" id="input" required>
                </div>
                <button type="submit" class="btn btn-outline-info">Lancer</button>
        </form>
    </div>
    <div class="col-6">
        {% if input is not empty %}
            <h2>Le resulat du lancer est  {{ input.result }} </h2>           
        {% endif %}
    </div>
</div>
{% endblock %}